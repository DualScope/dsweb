{
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "scripts": {
    "start": "npm start --prefix my-app",
    "build": "npm run build --prefix my-app",
    "deploy": "npm run deploy --prefix my-app"
  }
}
